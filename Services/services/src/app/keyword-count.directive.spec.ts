import { KeywordCountDirective } from './keyword-count.directive';

describe('KeywordCountDirective', () => {
  it('should create an instance', () => {
    const directive = new KeywordCountDirective();
    expect(directive).toBeTruthy();
  });
});
